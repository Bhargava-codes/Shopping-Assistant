.env
__pycache__/
*.py[cod]
.venv/

# Evaluation traces are local run output. Keep only .gitkeep so the directory exists.
eval/results/*
!eval/results/.gitkeep
.gstack/

# Local interviewer-only notes should never be committed or published.
.interviewer-private/

# Private holdout test set — kept local only, never committed or published.
data/holdout_cases.json

# Design concept asset — not part of the candidate exercise.
design/

# ElecKart storefront event log — local runtime output, not a fixture.
storefront_events.jsonl
